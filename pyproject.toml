[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
[project]
name = "verwaltungstool"
version = "0.4.0"
description = "Verwaltungstool"
dependencies = [
"python-dotenv",
"supabase",
]

[tool.setuptools]
packages = ["verwaltungstool"]