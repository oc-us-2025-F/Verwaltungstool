[] from verwaltungstool.supabase_client import supabase 

[] Checken, ob das User-Objekt verfügbar ist (user = supabase.auth.get_user())

[] Tabelle in supabase anlegen und RLS (Row Level Security) konfigurieren

[ ] beim Laden und Speichern auf supabase umstellen
